[pytest]
# pythonpath=. để `pytest` chạy được y hệt `python -m pytest`
# (các test import `backend.*` và `tests.conftest`).
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
